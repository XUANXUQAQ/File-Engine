package FileEngine.taskHandler.impl.frame.searchBar;

import FileEngine.taskHandler.Task;

public class SearchBarColorTask extends Task {
    public final int color;

    protected SearchBarColorTask(int color) {
        super();
        this.color = color;
    }
}
